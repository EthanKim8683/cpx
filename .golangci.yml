version: "2"

linters:
  default: standard
  enable:
    - revive
  settings:
    revive:
      rules:
        - name: exported
        - name: package-comments
  exclusions:
    presets:
      - common-false-positives
      - legacy
      - std-error-handling

run:
  timeout: 5m
